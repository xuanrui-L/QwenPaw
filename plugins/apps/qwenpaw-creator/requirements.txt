# QwenPaw supplies the shared framework/runtime dependencies. Keep only
# Creator-specific, non-database packages that are absent from the host
# lockfile here. Creator persistence is filesystem-native.
oss2
imageio-ffmpeg
# Document reading (vendored mm_plugins renderers): PDF page rendering,
# table/data rendering and XLSX ingestion. Notebook (nbformat) and SVG
# (resvg_py) renderers degrade gracefully and stay optional.
pypdfium2
matplotlib
pandas
openpyxl
# GIS (.geojson/.kml/.shp) and 3D model (.obj/.stl/.glb/...) renderers
# (vendored mm_plugins geo/model3d); both degrade to a clear missing-
# dependency error when absent.
geopandas
trimesh
# Deterministic HTML/CSS motion-overlay rendering (seek-and-capture).
# The Python package does not include a browser binary. Developer installs
# run `scripts/dev.sh install`, which idempotently installs Chromium as well.
# Other deployments must run `python -m playwright install chromium` once;
# motion overlays degrade gracefully when the browser runtime is unavailable.
playwright
