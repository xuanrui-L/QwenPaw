# no trailing slash so the dev-install symlink form is ignored too
ui/node_modules
# build output stays out of git; run `npm run build` in ui/ before installing
ui/dist/
.env
__pycache__/
*.pyc
data/
generated/
backend/data/
backend/generated/
