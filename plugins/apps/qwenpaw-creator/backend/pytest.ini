[pytest]
markers =
    unit: 纯函数/服务单测，零外部调用
    integration: FastAPI TestClient + 假模型的端点契约与工作流集测
    manual_real: 真实模型/真实素材的人工验收测试，默认排除，显式 -m manual_real 运行
testpaths = tests
addopts = -ra -m "not manual_real"
