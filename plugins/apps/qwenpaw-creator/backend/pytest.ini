[pytest]
markers =
    unit: 纯函数/服务单测，零外部调用
    integration: FastAPI TestClient + 假模型的端点契约与工作流集测
    manual_real: 真实调用验收测试（本地渲染/真实模型，含计费调用），默认 skip，需显式 -m 选择或环境变量（如 CREATOR_ASR_REAL_TEST=1）
testpaths = tests
addopts = -ra -m "not manual_real"
