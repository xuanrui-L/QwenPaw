# 空闲/等待时间分析 — project-70d9f4e47a025bbbb9eda7cb7dea7a8f

## 总览

| 指标 | 值 |
|------|-----|
| 项目总墙钟时间 | ~129 min（11:20:25 → 13:29:20） |
| 活跃处理时间 | ~40 min |
| **空闲时间** | **~89 min（69%）** |
| 其中 Bug 导致的空闲 | **~84 min** |
| 正常处理延迟 | ~5 min |

---

## 空闲时间线

| # | 时间段 | 时长 | 分类 | 严重程度 |
|---|--------|------|------|----------|
| 1 | 11:29:00 → 11:30:10 | 70s | TTS → Agent 恢复间隙 | 低 |
| 2 | **11:47:42 → 12:11:56** | **24 min** | **调度器停滞** | **高** |
| 3 | **12:12:27 → 13:12:26** | **60 min** | **调度器完全休眠** | **严重** |
| 4 | 13:12:26 → 13:14:05 | 1.5 min | 锁竞争 + 用户交互延迟 | 低 |
| 5 | 13:20:06 → 13:21:58 | 2 min | VLM/Agent 处理延迟 | 低 |

---

## 详细分析

### 空闲 #1：11:47:42 → 12:11:56（24 min）— Bug

**之前发生了什么：**
- 最后一个审阅目标（`goal-aa6a5d85`，分镜视频审阅 1/2 轮）在 11:47:42 完成
- 此前从 11:31 到 11:47 连续运行了 ~15 个审阅目标，每个 30-90s 完成

**期间发生了什么：**
- **完全沉默**：零日志条目，无调度器 tick，无 agent run，无任务派发，无锁活动
- 工作图调度器停止了运作

**什么触发了恢复：**
- 用户在 12:11:56 发送消息："执行的怎么样了？进度如何"
- 消息被接纳到 session，创建新目标，agent run 在 12:12:07 启动

**根因：**
工作图调度器在完成审阅循环后**静默停滞**。调度器一直在快速派发目标（每 30-90s 一个），最后一个审阅目标完成后，它未能检查或派发下一批工作项。没有错误日志、没有崩溃、没有异常——调度器只是停止了 tick。

**证据：**
- `state.json` 显示 `active_goal_id: null`，系统认为自己是空闲的
- 24 分钟间隙内无调度器 tick 日志
- 工作图有待处理的工作（elem:mask 视频生成），但在此期间从未被派发

---

### 空闲 #2：12:12:27 → 13:12:26（60 min）— Bug（最严重）

**之前发生了什么：**
- Agent 在 12:12:07 响应了用户的检查消息，读取了项目，在 12:12:27 完成状态检查目标
- Agent 的响应可能告诉用户"一切正常"，但没有实际触发任何新工作

**期间发生了什么：**
- **完全沉默 60 分钟**：零日志条目，无调度器活动，无 agent run，无任务派发
- 系统完全休眠

**什么触发了恢复：**
- 13:12:26 出现锁获取警告——项目锁在 0.55s 后获取（可能是后台健康检查或客户端轮询）
- 13:14:05 用户发送第二条消息："stale是旧状态吗？"
- 13:14:22 调度器终于派发了 `elem:mask` 视频生成任务

**关键观察：**
`elem:mask` R2V 视频生成任务在 13:14:22 创建，13:16:08 完成——实际工作不到 2 分钟。这意味着工作早已就绪且可执行，纯粹是调度器未能派发导致了 60 分钟空闲。

**根因：**
与空闲 #1 相同的底层 Bug，但更严重。Agent 完成状态检查目标后，调度器再次未能派发待处理的工作图节点。系统进入完全休眠状态。

---

### 空闲 #3：13:12:26 → 13:14:05（1.5 min）— 锁竞争

**分析：**
- 13:12:26 的锁获取唤醒了某些东西，但调度器直到 13:14:22 才派发 elem:mask 任务
- 13:14:05 的用户消息似乎是最终触发器
- 1.5 分钟间隙包括锁竞争（锁获取耗时 0.55s）和用户消息传播时间

**分类：** 轻微。部分是预期的（用户交互延迟），部分是锁竞争。

---

## elem:mask 任务之谜

**问题：** 为什么 elem:mask 任务从 11:32:00 到 12:17:14（~45 min）后才被"取消"？

**分析：**
日志中**没有**显示 11:32 到 12:17 之间有任何 elem:mask 任务的创建或执行。实际发生的是：
- 11:32-11:47：多个审阅循环目标快速运行
- 11:47-12:11：调度器完全停滞（空闲 #1）
- 12:11-12:12：用户检查，agent 状态响应

elem:mask R2V 视频生成任务实际**创建时间是 13:14:22**，**成功时间是 13:16:08**——从未被取消。

**真相：** elem:mask 元素从 ~11:32（首次被识别需要视频生成）到 13:14:22（最终被派发）期间一直"卡"在待处理/排队状态。~1h42m 的"等待"不是单个任务在运行，而是工作图调度器在整个期间未能派发该任务。任务本身一旦派发，不到 2 分钟就完成了。

---

## 锁竞争分析

### 严重锁错误（同线程嵌套死锁防护）：

| 时间戳 | 详情 |
|--------|------|
| 11:26:31 | `list_specialist_runs` API 调用尝试获取读锁，但同线程已持有排他锁。请求失败（500）。 |
| 11:32:48 | `append_event` 在 specialist run 回调期间尝试获取锁，但同线程已持有排他锁。导致 `specialist-run-a8b11ca8`（visual_development_agent）**完全失败**。 |
| 11:36:50 | 同模式：`list_specialist_runs` API 调用嵌套锁尝试。 |

### 锁获取警告（慢获取）：

| 时间戳 | 等待时间 | 锁路径 |
|--------|----------|--------|
| 11:33:32 | 0.97s | execution-runtime.lock |
| 11:36:56 | 1.06s | project lock |
| 11:41:37 | 2.58s | session-runtime.lock |
| 11:41:40 | 0.50s | session-runtime.lock |
| 11:41:42 | 1.46s | session-runtime.lock |
| 11:41:47 | 0.77s | project lock |
| 11:41:48 | 0.92s | session-runtime.lock |
| 11:41:53 | 0.66s | session-runtime.lock |
| 13:12:26 | 0.55s | project lock |
| 13:16:14 | 1.15-1.19s | project lock（多个） |
| 13:16:14 | 0.53s | execution-runtime.lock |
| 13:17:18 | 0.50-1.08s | session-runtime + project locks |
| **13:22:09** | **10.00s（超时）** | **project lock — 导致 compose 任务失败** |
| 13:25:46 | 0.71-0.76s | project lock（多个） |

**锁竞争影响：**
- 13:22:09 的 10 秒锁超时导致 `task-b380f918`（compose）失败。调度器重试（retry 1/6）后成功。
- 11:26:31 和 11:32:48 的锁错误导致直接失败且无重试。
- 嵌套锁死锁检测正常工作（防止实际死锁），但底层问题是同一线程（asyncio 事件循环线程）尝试重入锁获取，锁实现正确地拒绝了。

---

## 分类总结

| 空闲时段 | 预期还是 Bug？ | 严重程度 |
|---------|---------------|----------|
| 11:47:42 → 12:11:56（24 min） | **Bug** | 高 — 调度器静默停止派发 |
| 12:12:27 → 13:12:26（60 min） | **Bug** | 严重 — 系统完全休眠，有待处理工作 |
| 13:12:26 → 13:14:05（1.5 min） | 预期（用户交互延迟） | 低 |
| 13:20:06 → 13:21:58（2 min） | 预期（VLM/Agent 处理） | 低 |

---

## 建议

### 严重（修复 84 分钟空闲）：

1. **工作图调度器派发循环需要看门狗/心跳**：调度器应有周期性 tick（如每 5-10 秒），即使没有活跃目标也检查可派发的工作图节点。当前设计似乎只在目标完成事件触发时派发，这意味着如果最后一批目标的完成没有产生新的可派发节点（因为它们依赖外部条件或处于不同阶段），调度器就会静默。

2. **添加调度器停滞检测**：如果 >60 秒没有调度器 tick 同时存在待处理（QUEUED）任务或未处理的工作图节点，记录 WARNING。如果 >120 秒，尝试强制重启派发循环。

3. **用户消息不应是解除调度器卡住的唯一方式**：两个主要空闲时段都只在用户交互后结束，表明调度器没有自愈机制。添加周期性后台任务检查停滞的调度器并重启它们。

### 高（修复锁竞争）：

4. **修复同线程嵌套锁获取**：11:26:31、11:32:48、11:36:50 的三个 `RuntimeFileValidationError` 错误是由同一 asyncio 线程尝试重新获取它已持有的锁引起的。`store.py:626` 的 `lifecycle_lock` 持有排他锁，然后该作用域内的代码路径（如 `append_event`、`list_specialist_runs`）尝试再次获取同一锁。要么：
   - 使锁对同一线程可重入，或
   - 重构代码使嵌套调用不尝试重新获取锁（如使用内部无锁版本的方法）

5. **减少锁持有时间**：多个锁获取耗时 >1s。项目锁不应在其它线程需要访问期间长时间持有。考虑更细粒度的锁或更短的临界区。

### 中等：

6. **向可观测性添加空闲时间监控**：日志文件没有周期性的"调度器存活"心跳条目。从调度器循环每 30 秒添加 DEBUG 级别日志，使空闲时间在日志中可见，而不是表现为神秘的间隙。

7. **审查审阅循环**：11:31 到 11:47 之间，系统快速运行了 ~15 个审阅目标。每个审阅目标完成后立即生成下一个。考虑是否所有这些审阅都是必要的，或者是否可以批处理/并行化以减少总循环时间。
