# -*- coding: utf-8 -*-
"""Deprecated structured-browser escape hatch.

This package preserves the stable ``browser`` implementation while the
experimental unified browser gathers beta feedback.  It must not import the
unified browser SDK; the tools package chooses exactly one implementation.
"""
